// categories, expenses, login, register
// categories should have an id, expenses should have an id
// login should have an id

function loadCategories() {
  // TODO
  // a div should prompt for a category
  const div = document.getElementById("categories");
  div.innerHTML = "Category";
}
